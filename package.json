{
  "name": "learnwy-skills",
  "version": "1.0.0",
  "private": true,
  "description": "Personal skills repository with rslib-bundled CLI scripts",
  "type": "module",
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "build": "rslib build",
    "watch": "rslib build --watch",
    "typecheck": "tsc --noEmit",
    "test": "rstest run",
    "check": "pnpm run typecheck && pnpm run build && pnpm run test",
    "prepare": "git config core.hooksPath .githooks",
    "release": "git push origin main && pnpm dlx skills install -g -y learnwy/skills && pnpm run install:hooks",
    "install:hooks": "node scripts/manage-hooks.mjs install",
    "uninstall:hooks": "node scripts/manage-hooks.mjs uninstall"
  },
  "devDependencies": {
    "@rslib/core": "^0.10.0",
    "@rstest/adapter-rslib": "^0.2.2",
    "@rstest/core": "^0.9.10",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
